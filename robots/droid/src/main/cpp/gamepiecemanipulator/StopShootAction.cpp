#include "StopShootAction.h"
#include <gamepiecemanipulator/GamePieceManipulator.h>

namespace xero {
    namespace droid {

        StopShootAction::StopShootAction(GamePieceManipulator &subsystem) : GamePieceManipulatorAction(subsystem)
        {
        }

        StopShootAction::~StopShootAction()
        {
        }

        void StopShootAction::start() 
        {
        }

        void StopShootAction::run() 
        {
        }
    }
}