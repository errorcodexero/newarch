{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Simulator - Phaser",
            "type": "shell",
            "command": "c:\\cygwin64\\bin\\bash.exe -c 'PATH=/bin:/usr/bin ; cd robots/phaser ; make '",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ]
        },
        {
            "label": "Build Simulator - Phaser Deploy",
            "type": "shell",
            "command": "c:\\cygwin64\\bin\\bash.exe -c 'PATH=/bin:/usr/bin ; cd robots/phaser ; make deploy'"
        }
    ]
}