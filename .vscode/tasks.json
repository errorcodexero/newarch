{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Simulator - Phoenix",
            "type": "shell",
            "command": "c:\\cygwin64\\bin\\bash.exe -c 'PATH=/bin:/usr/bin ; cd robots/phoenix ; make apponly'",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ]
        },
        {
            "label": "Build Simulator - Phoenix Deploy",
            "type": "shell",
            "command": "c:\\cygwin64\\bin\\bash.exe -c 'PATH=/bin:/usr/bin ; cd robots/phoenix ; make deploy'"
        }
    ]
}